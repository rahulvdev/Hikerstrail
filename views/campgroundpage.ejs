<% include ./partials/header %>
<div class="container">
    <div class="row">
        <div class="col-md-3">
            <p class="lead">YelpCamp</p>
            <div class="list-group">
                <li class="list-group-item active">Info 1</li>
                <li class="list-group-item">Info 2</li>
                <li class="list-group-item">Info 3</li>
            </div>
        </div>
        <div class="col-md-9">
            <div class="thumbnail">
                <img class="img-responsive indivCamp" src="<%= camp.image %>">
                <div class="caption-full">
                    <h4><%=camp.name %>,submitted by <%= camp.author.name%></h4>
                    <% if(currentUser && camp.author.id.equals(currentUser._id)){%>
                    <a href="/campgrounds/<%= camp._id%>/edit"role="button" class="btn btn-warning btn-md">Edit</a>
                    <form class="delete-form" action="/campgrounds/<%= camp._id%>?_method=DELETE" method="POST">
                       <button class="btn btn-danger">Delete</button>
                       <% } %>
                    </form>
                    <p><%= camp.description %></p>
                </div>
            </div>
            <div class="well">
                <div class="text-right">
                    <a class="btn btn-success" href="/campgrounds/<%= camp._id %>/comments/new">Add New Comment</a>
                </div>
                <hr>
                <% camp.comments.forEach(function(comment){ %>
                    <div class="row">
                        <div class="col-md-12">
                            <strong><%= comment.author.name %></strong>
                            <span class="pull-right">10 days ago</span>
                            <p>
                                <%= comment.post %> 
                            </p>
                            <% if(currentUser && comment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-warning btn-xs" href="/campgrounds/<%= camp._id %>/comments/<%= comment._id %>/edit">Edit</a>
                            <form class="delete-form" action="/campgrounds/<%= camp._id %>/comments/<%= comment._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-danger btn-xs">Delete</a>
                            </form>
                            <% }%>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</div>

<% include ./partials/footer %>